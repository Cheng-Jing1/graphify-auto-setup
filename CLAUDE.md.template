# Graphify 自动化规则

将此文件复制到项目根目录的 `CLAUDE.md` 中，或将其内容追加到已有的 `CLAUDE.md` 中。

## 规则

### 1. 项目初始化 — 自动建图

当进入一个新的项目（首次会话，且未检测到 `graphify-out/graph.json`）时：

- **小型项目**（< 50 个文件 且 < 10,000 行代码）→ 自动运行 `/graphify` 建图，不需要询问
- **大型项目** → 主动询问用户："检测到大型项目，是否要建图？这将消耗 Token 但后续查询可大幅节省。"

判断方式：扫描 `src/`、`lib/`、`app/` 等目录，统计文件数量和代码行数。

### 2. 代码查询 — 自动查图

当用户询问关于项目架构、模块关系、代码依赖等问题时，自动检查 `graphify-out/graph.json` 是否存在。如果存在，使用图查询来回答，而不是重新读取源文件。

查询方式优先级：
1. 优先使用 `/graphify query "xxx"` 做 BFS 遍历获取上下文
2. 需要精确路径时使用 `/graphify path "A" "B"`
3. 需要解释单个节点时使用 `/graphify explain "xxx"`

### 3. 代码变更后 — 自动增量更新

检测到用户完成代码修改（如创建/修改/删除文件）后：
- 自动运行 `/graphify <项目路径> --update` 增量更新图
- 如果变更的是纯代码文件，AST 提取不需要 LLM Token

### 4. 会话持久化规则

保存对 graphify 输出目录的记忆，以便跨会话复用已有的图：

```
# memory: graphify
# graphify-out/graph.json 包含项目的知识图谱
# 回答架构问题前先查图
```
